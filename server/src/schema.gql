# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

"""song"""
type Song {
  id: ID!
  title: String!
  description: String!
  downloadLink: String!
  listens: [Listen]!
  artist: Artist!
}

"""listen"""
type Listen {
  id: ID!
  artist: Artist!
  user: User!
  song: Song!
  liked: Boolean!
}

"""artist"""
type Artist {
  id: ID!
  stageName: String!
  bio: String!
  image: String!
  listens: [Listen]!
  songs: [Song]!
  user: User!
}

"""user"""
type User {
  id: ID!
  username: String!
  email: String!
  firstName: String!
  lastName: String
  artist: Artist
  listens: [Listen]!
}

type Query {
  song(id: String!): Song
  findUnheardSong(userId: String!): Song
  listen(id: String!): Listen
  artist(id: String!): Artist
  user(id: String!): User
}

type Mutation {
  listenToSong(listenToSongData: ListenToSongInput!): String!
  createArtist(createArtistData: CreateArtistInput!): Artist!
  updateArtist(artistId: String!, updateArtistData: UpdateArtistInput!): Artist!
  createUser(createUserData: CreateUserInput!): User!
  updateUser(updateUserData: UpdateUserInput!): User!
}

input ListenToSongInput {
  songId: ID!
  artistId: ID!
  liked: Boolean!
}

input CreateArtistInput {
  stageName: String!
  bio: String!
  image: String!
}

input UpdateArtistInput {
  stageName: String
  bio: String
  image: String
}

input CreateUserInput {
  username: String!
  password: String!
  email: String!
  firstName: String!
  lastName: String
}

input UpdateUserInput {
  username: String
  password: String
  firstName: String
  lastName: String
}