# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

"""song"""
type Song {
  id: ID!
  title: String!
  description: String!
  downloadLink: String!
  listens: [Listen]!
}

"""listen"""
type Listen {
  id: ID!
  artist: Artist!
  song: Song!
  liked: Boolean!
}

"""artist"""
type Artist {
  id: ID!
  stageName: String!
  bio: String!
  image: String!
  listens: [Listen]!
  songs: [Song]!
  user: User!
}

"""user"""
type User {
  id: ID!
  username: String!
  email: String!
  firstName: String!
  lastName: String
  artist: Artist!
  listens: [Listen]!
}

type Query {
  song(id: String!): Song
  listen(userId: String!): Listen!
  user(id: String!): User
  artist(id: String!): Artist
}