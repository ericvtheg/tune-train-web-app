# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

"""song"""
type Song {
  id: ID!
  title: String!
  description: String!
  downloadLink: String!
  listens: [Listen]!
  artist: Artist!
}

"""listen"""
type Listen {
  id: ID!
  artist: Artist!
  song: Song!
  liked: Boolean!
  user: User!
}

"""artist"""
type Artist {
  id: ID!
  stageName: String!
  bio: String!
  image: String!
  listens: [Listen]!
  songs: [Song]!
  user: User!
}

"""user"""
type User {
  id: ID!
  username: String!
  email: String!
  firstName: String!
  lastName: String
  artist: Artist
  listens: [Listen]!
}

type Query {
  song(id: String!): Song
  findUnheardSong(userId: String!): Song
  listen(id: String!): Listen
  artist(id: String!): Artist
  user(id: String!): User
}

type Mutation {
  listenToSong(listenToSongData: ListenToSongInput!): Boolean!
}

input ListenToSongInput {
  songId: ID!
  artistId: ID!
  liked: Boolean!
}